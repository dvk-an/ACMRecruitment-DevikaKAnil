n=int(input('enter integer: '))
r=0
neg=n<0
if neg:
    n=-n 
while n>0:
    x=n%10
    r=r*10+x
    n=n//10
    #to remove last digit from entered integer
if neg:
    r=-r 
print('Reversed integer:',r)
